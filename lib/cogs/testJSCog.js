/* 
In-progress test to see if bot would integrate JS with Python.
Inconclusive, will have to figure out how discord.js works.

==============================================================
var bot = {};

var JSTest = function() {
    MSGesture.channel.send("JS Cog Test successful");
}

var setup = function (b) {
    bot = b;
    bot.registerCommand("jstest", JSTest);
}

exports.requires = [];
exports.setup = setup;
*/